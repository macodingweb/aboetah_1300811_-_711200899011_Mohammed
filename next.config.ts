import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  output: 'export',
  reactStrictMode: true, // مثال على إعداد آخر إذا كنت تستخدمه
  // أي إعدادات إضافية هنا
};

export default nextConfig;
